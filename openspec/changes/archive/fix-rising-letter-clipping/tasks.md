## 1. Adjust Clipper Header Headroom
- [x] 1.1 Update `.envelope-pocket-clipper` inset in [creator.css](file:///home/fenix3819/sigil-and-script/src/styles/creator.css) from `inset: -150px -50px 165px -50px` to `inset: -250px -50px 165px -50px`.

## 2. Verification
- [x] 2.1 Run tests with `npm run test` to verify no component or reducer test regressions.
- [x] 2.2 Run `npm run build` to confirm compilation.
