## 1. Edit SectionEditor Layout and Store Hook
- [x] 1.1 Update [SectionEditor.tsx](file:///home/fenix3819/sigil-and-script/src/components/creator/SectionEditor.tsx) to destructure `updateTextBlock` from the `useSigil` context.
- [x] 1.2 Implement the Host Names input field at the top of the layout in [SectionEditor.tsx](file:///home/fenix3819/sigil-and-script/src/components/creator/SectionEditor.tsx) mapped to the `tb-headline` text block's content.

## 2. Verification
- [x] 2.1 Run unit tests with `npm run test` to verify no regressions.
- [x] 2.2 Run `npm run build` to confirm compiling.
